/* CHALLNEGE:
* Find a way to return 'I can write numbers, 1, 2, 3' without using numbers or length
*/

var anarchy = function () {
  var one = Math.PI / Math.PI;
  var two = one + one;
  var three = two + one;
  return 'I can write numbers like, ' + one + ', ' + two + ', ' + three + '.';
}